// const API_BASE = import.meta.env.VITE_API_BASE; // e.g. https://your-backend.up.railway.app
// export const shorten = (url: string) =>
//  fetch(`${API_BASE}/shorten`, { method: "POST", body: url }).then(r => r.text());